@if (isloading)
{
    <img src="/assets/img/loading.gif" style="height:@(height)px;width:@(width)px;" />
}
@code {
    [Parameter] public bool isloading { get; set; } = false;
    [Parameter] public int height { get; set; } = 25;
    [Parameter] public int width { get; set; } = 25;
}
